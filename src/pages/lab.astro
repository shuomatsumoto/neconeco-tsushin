---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import ArticleList from "../components/ArticleList.astro";

const postsAll = await getCollection("posts", ({ data }) => !data.draft && data.category === "研究室");
const posts = postsAll.sort((a,b) => (a.data.date < b.data.date ? 1 : -1));
---
<BaseLayout title="研究室 📎 | かもねぎ通信">
  <div class="card">
    <h2 class="h2">研究室 📎</h2>
    <div class="small">専門分野の論文紹介・研究メモ。</div>
  </div>
  <div class="hr"></div>
  <ArticleList posts={posts} title="記事一覧" />
</BaseLayout>
