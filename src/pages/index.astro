---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import Calendar from "../components/Calendar.astro";
import ArticleList from "../components/ArticleList.astro";
import plans from "../data/plans.json";
import PlanList from "../components/PlanList.astro";

const postsAll = await getCollection("posts", ({ data }) => !data.draft);
const posts = postsAll.sort((a, b) => (a.data.date < b.data.date ? 1 : -1));
---
<BaseLayout title="かもねぎ通信">
  <div class="homeGrid">
    <main class="homeCol">
      <section class="homeSection">
        <ArticleList posts={posts} limit={10} title="最新の記事" />
      </section>

      <section class="homeSection">
        <Calendar posts={posts} plans={plans} />
      </section>
    </main>

    <aside class="homeCol homeAside">
      <section class="homeSection">
        <PlanList plans={plans} title="執筆予定" limit={8} />
      </section>

      <section class="homeSection">
        <h2 class="h2" style="margin:0;">🔗 SNSアカウント</h2>
        <div class="snsRow" role="list">
          <a class="item" href="https://x.com/neo_neco_">X</a>
          <a class="item" href="https://note.com/neoneco">Note</a>
          <a class="item" href="https://bookmeter.com/users/1179346">読メ</a>
          <a class="item" href="https://www.youtube.com/@shuomatsumoto">YouTube</a>
        </div>
      </section>
    </aside>
  </div>

  <style>
    .homeGrid {
      display: grid;
      grid-template-columns: 1.3fr 0.85fr;
      gap: 16px;
    }

    .homeCol {
      display: grid;
      gap: 16px;
      align-content: start;
      min-width: 0;
    }

    .homeAside {
      padding-left: 14px;
      border-left: 1px solid rgba(0,0,0,0.06);
    }

    .homeSection {
      padding: 0;
      margin: 0;
    }

    /* SNS */
    .snsRow {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-top: 10px;
    }

    .snsRow .item {
      text-decoration: none;
      color: var(--ink);
      padding: 5px 10px;
      border: 1px solid rgba(0,0,0,0.10);
      border-radius: 999px;
      font-size: 0.88em;
      line-height: 1.1;
      background: transparent;
    }

    .snsRow .item:hover {
      opacity: 0.85;
    }

    @media (max-width: 900px) {
      .homeGrid {
        grid-template-columns: 1fr;
      }
      .homeAside {
        padding-left: 0;
        border-left: 0;
      }
    }
  </style>
</BaseLayout>
